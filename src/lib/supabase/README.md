# Supabase browser client

`client.ts` membaca `.env.local` dan memakai generated `database.types.ts`.
Baca `LOGIN_SETUP_ID.md` untuk konfigurasi dan uji login. Jangan mengedit tipe
generated secara manual. Tidak ada SQL atau migrasi yang dijalankan oleh file ini.
